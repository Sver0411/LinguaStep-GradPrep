# LinguaStep（日英阶梯）

[English README](README.en.md)

> 日语与英语，一起稳步进阶

LinguaStep 是一个中文界面的个人日语与英语学习网页应用，面向日语 JLPT N3/N2/N1、英语四级/六级/TOEIC 的长期学习目标。它把日英对应词卡、语法、复习计划、真题式练习、错题、收藏和统计整合在同一个学习闭环中。

项目当前版本为 **v0.7.4**。无需注册账号；内置内容和学习记录保存在当前浏览器，断网时仍可继续核心学习。当前前端已暂停并移除 AI 入口，保留相关底层代码和既有内容数据，后续需要时可以重新启用。

公开站点：[打开 LinguaStep](https://twclab.top/LinguaStep)

## 项目特点

- **完整而非原型**：学习、复习、测试、错题和进度保存都是真实可操作功能。
- **日英同步学习**：同一中文语义下同时理解日语与英语，也可以只学其中一种语言。
- **本地优先**：无需登录，IndexedDB 保存结构化学习数据，核心功能不依赖网络。
- **长期复习**：使用可解释的间隔重复调度和每日计划，不只浏览静态词表。
- **内容可直接学习**：内置 2559 组人工整理或教材对齐的考试词（N1/N2/N3 每级至少 800 词）、80 个独立语法点和 137 组日英语法对比。
- **真题式练习**：日语按 N3/N2/N1 分级，英语按四级/六级/TOEIC 分级，题目与词库学习相互独立。
- **手机端独立体验**：电脑端继续使用侧边栏；手机端采用独立信息层级、固定底部导航、筛选抽屉和单主操作页面，不再把桌面内容压缩堆叠。

## 功能总览

### 首页与每日计划

- 自动生成当天的新词、到期复习、错题、语法和测试任务。
- 同一天刷新或多标签页打开不会重复创建计划。
- 首页只保留今日进度、逾期复习和活跃错题三个核心指标，并提供唯一的“继续”主按钮。
- 固定主线为：到期复习 → 今日新词 → 今日语法 → 今日测试 → 本次错题巩固 → 今日完成。
- 每轮完成页直接进入下一项，不必反复返回首页重新选择。
- 根据当前进度推荐下一步，可从首页“更多”按最新设置重新计算当天计划。
- 支持自动补足未完成任务，以及周末轻量、不变或加强三种节奏。
- 保留自由学习入口，不强制用户按计划学习。

### 单词学习

- 内置 **2559 组**日英对应词，覆盖 N3/N2/N1 与四级/六级/TOEIC 常用考试范围，且 N1/N2/N3 每级至少 800 词；本轮新增词从本地红宝书、绿宝书词汇单元 OCR 对齐并以已校订扩展词库补足。
- 每组包含中文核心义、日语写法/假名/罗马音、英语单词/音标、词性、等级、例句、翻译、搭配和差异说明。
- 日语普通动词统一使用词典原形；「勉強する／確認する」等サ变动词以「勉強／確認」词干收录，并在词性和说明中保留可接「する」的信息。
- 支持三种独立学习轨迹：
  - 日英对照
  - 只学日语
  - 只学英语
- 页面分成“开始学习”和“浏览词库”；搜索筛选不会干扰日常学习入口。
- 支持简洁版与完整版、同时揭示与分步揭示、日语优先/英语优先/随机揭示；默认值统一在设置页管理。
- 可选择 10、20、30 或自定义轮次，完成后查看认识、模糊、不认识和掌握比例。
- 每次开始学习都会重新随机抽取并打乱顺序，同时保持未学词优先；复习、收藏和今日新词队列也会随机排列。
- 一轮结束后可以直接继续剩余今日新词；“重练生词”只重新练习本轮标记为“不认识”的词。
- 支持中文、日语汉字、假名、罗马音、英语、例句和搭配搜索。
- 开始学习页先选日语、英语或日英混合，再显示相应难度；浏览词库页支持按日语等级、英语等级和掌握状态筛选。

学习快捷键：

| 按键 | 操作 |
| --- | --- |
| `Space` | 揭示答案 |
| `1` | 认识 |
| `2` | 模糊 |
| `3` | 不认识 |
| `←` / `→` | 上一张 / 下一张 |
| `Esc` | 退出专注模式 |

### 间隔重复复习

LinguaStep 使用可解释的 **SM-2 衍生调度器**，而不是宣称实现完整 FSRS。三种反馈会产生不同的复习间隔：

| 反馈 | 主要效果 |
| --- | --- |
| 认识 | 延长间隔，提高连续正确次数；连续认识后进入已掌握 |
| 模糊 | 约 1 天后再次复习，降低稳定度 |
| 不认识 | 约 10 分钟后重现，提高遗忘次数和优先级 |

复习状态包含首次/最近学习时间、下次复习时间、复习次数、连续正确、遗忘次数、稳定度、难度、暂停状态、语言模式和算法版本。队列优先处理逾期错题、逾期内容、不认识、模糊、今日到期和新内容。

### 语法与日英对比

- 内置 **80 个语法点**：65 个日语语法、15 个英语语法；本轮新增 30 个日语句型来自本地蓝宝书 N3 文法目录与讲解页。
- 日语覆盖 N3 巩固、N2 核心和少量 N1 过渡；英语覆盖高中、四级和少量六级过渡。
- 每个语法点包含中文讲解、结构、接续/用法、场景、语气、例句、翻译、常见错误、易混点和 5 道四选一练习。
- 内置 **137 组日英语法对比**，覆盖基础句型、时态体、条件、推测、原因、让步、敬语以及 N2/N1 正式表达，不强行逐字对应。
- 日英对比可一键进入混合测试；每轮从不同对比项抽题，不再只练当前一条。
- 语法支持未学习、学习中、待复习、已掌握状态；练习答错后会降低掌握度并进入错题和后续复习。
- 默认优先展示今日计划语法；完整检索和筛选按需展开。
- 支持按关键词、语言、等级、状态、收藏、错题和到期状态搜索筛选。

### 真题练习

- 测试页先选择日语或英语，再选择难度；日语额外选择文字、文法或阅读。
- 日语 N3/N2/N1 的文字、词汇和文法题以本地《红蓝宝书1000题 N5-N1》资料为基础，并补充内置分级题；阅读题是同等级原创真题式材料，并在界面中明确标注来源。
- 日语和英语在每个难度等级下，文字、文法、阅读三个题型均各有 **120 题**，不是把总题数简单凑到 120。
- 英语提供四级、六级、TOEIC 三套原创考试型综合题，混合词汇、语法和阅读。
- 每个等级提供独立题库，题目随机打散，不依赖已学词条，也不会把同一个词的多个翻译方向重复塞进一轮。
- 开始前可选择 10、20、30 或 50 题；每次开始和“再做一组”都会从当前等级与题型的完整题库重新随机抽取。
- 每轮会重新排列四个选项，并将正确答案随机、均衡地分布到 A/B/C/D；支持按 1–4 选答案，选中后按 Space 进入下一题或提交。
- 练习中始终提供“退出测试”，完成后展示得分、分类正确率、用时和逐题解析；错误答案会进入错题本。

### 错题本

- 按单词、日语语法、英语语法和日英对比分组管理。
- 状态生命周期：活跃错题 → 巩固中 → 已掌握 → 已归档。
- 记录错误次数、连续答对、最近错误时间、来源和历史选项快照。
- 默认连续答对 3 次后移出活跃错题，也可在设置中调整。
- “开始复习活跃错题”会按优先级连续进入下一题，不再反复返回列表。
- 支持专项重练、标记掌握、重新加入、归档、移出和收藏；管理操作默认收起。
- 错题解析以本地题目解释为主；AI 入口当前隐藏，不会影响错题复习。

### 收藏

- 可收藏单词、语法和日英语法对比。
- 支持关键词、语言和难度筛选。
- 可以从收藏内容直接开始学习或生成专项测试。
- 支持批量取消收藏，并提供确认保护。

### 学习统计

- 展示今日、本周、累计学习量以及已学/已掌握单词和语法。
- 展示测试次数、总体正确率、当前/最长连续学习天数、待复习和活跃错题。
- 提供最近 7 天和 30 天的学习量、正确率、新学/复习、日语/英语趋势。
- 展示日语、英语、日英对照和语法的掌握分布。
- 图表兼容浅色、深色和手机屏幕，同时提供文字数字，不只依赖颜色和图形。

### AI 状态

AI 入口目前已从前端导航、首页、单词、语法、测试和设置页移除，避免影响个人学习动线。仓库保留后端兼容接口和历史 AI 数据模型，但默认不会调用外部 AI 服务；重新启用前端入口时需要重新进行安全审查。

### 设置、主题与设备适配

- 学习设置：默认模式、揭示顺序、每日新词/复习/语法/测试数量、每轮数量、错题优先和周末节奏。
- 显示设置：学习专注模式开关；浅色、深色、跟随系统；简洁/完整；标准/较大字体；动画和减少动态效果。
- 数据设置：分别重置学习进度、测试、错题、收藏或全部数据，危险操作均需确认。
- 电脑端使用侧边栏，手机端使用底部导航和“更多”抽屉；学习时可进入专注模式。
- v0.7.0 对手机端重新设计：首页只显示下一步，单词只保留本轮选择，语法先展示纵向列表，测试隐藏题库统计，错题/收藏收起管理操作，统计减少次要图表，设置按学习与外观分类切换。
- v0.7.1 为真题测试增加 10、20、30、50 题的本轮题量选择，并改为每次开始都重新随机抽题。
- v0.7.2 将手机端上下导航与页面滚动彻底分离，单词评分按钮固定在拇指区；专注模式离开学习会话后自动清理，首页补充近 7 天节奏与今日答题状态。
- v0.7.3 修复单词总结页继续与重练按钮无响应；“重练生词”只使用本轮不认识的词，手机端为固定评分区补足可滚动安全空间。
- v0.7.4 修复测试正确答案位置集中问题，每轮随机并均衡分布 A/B/C/D；新增选中答案后按 Space 下一题或提交。

## 推荐使用流程

1. 打开首页，点击唯一的“继续”按钮。
2. 应用依次带你完成到期复习、今日新词和今日语法。
3. 一键完成今日测试，再连续巩固本次活跃错题。
4. 今日完成后，可在统计页查看趋势，或展开自由学习入口。
5. 需要巩固薄弱点时，从错题本或收藏进入对应专项练习。

电脑端导航按“学习 / 复习 / 工具”分组；手机端保留“今日 / 单词 / 语法 / 测试 / 更多”五个固定入口。

## 技术栈与选择理由

| 技术 | 用途与选择理由 |
| --- | --- |
| React 19 + Next.js 16 | 成熟的组件、路由和服务端 API 能力，可本地运行并适配 Vercel |
| Vinext + Vite | 为 OpenAI Sites/Cloudflare 构建提供 Next.js 兼容运行路径和快速开发体验 |
| TypeScript strict | 集中约束学习、复习、存储、API 和 AI 内容模型，降低迁移错误 |
| 原生 CSS + Lucide React | 保持视觉系统可控、依赖轻量，并支持响应式、主题和可访问焦点 |
| IndexedDB + Repository Pattern | 在浏览器本地保存结构化学习数据，同时隔离 UI 与存储实现，便于未来替换云端仓库 |
| Zod | 校验服务端输入和 AI 结构化输出，阻止不完整内容进入数据库 |
| 原生 `fetch` | 接入 DeepSeek OpenAI-compatible API，避免引入庞大 AI 框架 |
| Vitest + Testing Library + fake-indexeddb | 覆盖纯业务逻辑、组件、接口、数据库迁移和完整 Mock 工作流 |

## 架构

```text
页面与组件
  ├─ LearningContext
  │    ├─ 学习、测试、计划、统计纯函数
  │    └─ LearningRepository → IndexedDB v3 / 内存降级
  └─ AIContext
       └─ AIAPIClient → React Server Action（默认）
                          └─ 请求保护与限流（/study-service/* 保留兼容）
                               └─ AIContentService
                                    ├─ Prompt 模板
                                    ├─ Zod 与业务校验
                                    └─ AIProvider
                                         ├─ DeepSeekProvider
                                         └─ MockAIProvider
```

页面不直接操作 IndexedDB，也不直接调用 DeepSeek。学习逻辑、存储、AI Provider、内容校验和 UI 分层独立；AI 保存的词汇、语法和对比会进入与内置内容相同的学习、搜索、收藏、测试和统计流程。

## 目录结构

```text
app/                    页面入口、布局和同源 AI API
components/             应用壳、学习组件、筛选器、图表和页面视图
context/                LearningContext 与 AIContext
data/                   2559 组精选考试词、80 个语法和 137 组对比
lib/                    模型、复习、计划、测试、统计与通用逻辑
lib/ai/                 AI 配置、Provider、Prompt、Schema、校验和服务
lib/repositories/       IndexedDB、迁移、Repository 与内存降级
tests/                  单元、接口、组件、迁移和工作流测试
docs/                   架构、算法、存储、安全、测试和部署文档
.openai/hosting.json    OpenAI Sites 项目配置
```

## 本地安装与运行

环境要求：Node.js `>= 22.13`。

```bash
npm install
npm run dev
```

默认地址通常是 `http://localhost:3000`。不配置任何 API Key 也可以使用全部本地学习功能。

本地生产构建与启动：

```bash
npm run build
npm start
```

## DeepSeek 配置

### 方式一：服务器托管 Key

这是自托管环境的推荐方式。复制环境变量模板，并把真实值只写入未提交的 `.env.local`：

```bash
cp .env.example .env.local
```

```env
AI_PROVIDER=deepseek
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_API_KEY=你的服务器密钥
DEEPSEEK_MODEL_FAST=deepseek-v4-flash
DEEPSEEK_MODEL_QUALITY=deepseek-v4-pro
AI_ENABLED=true
AI_TIMEOUT_MS=45000
AI_MAX_RETRIES=3
AI_MAX_CONCURRENCY=2
AI_PROXY_ACCESS_TOKEN=
```

本地 `localhost` 可以不设置代理访问令牌。公网部署使用服务器 Key 时，必须设置足够长且随机的 `AI_PROXY_ACCESS_TOKEN`，并在个人浏览器的 AI 设置中输入相同令牌；否则生产服务器模式会拒绝请求，避免公开网址消耗个人额度。

AI 后端兼容配置仍可通过环境变量保留，但当前产品界面不会展示或调用该能力。

### 方式二：用户自带 Key（BYOK，暂未开放）

在设置页选择“个人 API Key”。浏览器把 Key 放入专用请求头，按次发送到同源代理；服务端不保存、不返回，Key 不进入 URL、请求正文、IndexedDB、学习快照或 AI 历史。

公开站点当前不提供 BYOK 设置入口；相关说明仅用于后端兼容维护。

- 默认仅保存在 `sessionStorage`，浏览器会话结束后失效。
- “保存在此设备”使用 `localStorage`，只应在可信个人设备上启用。
- 浏览器不会直接跨域请求 DeepSeek。

当前默认模型：

- Fast：`deepseek-v4-flash`，用于普通结构化生成。
- Quality：`deepseek-v4-pro`，用于复杂语法、日英对比、错题解释或质量复核。
- API 基址：`https://api.deepseek.com`。

模型和接口以 DeepSeek 官方文档为准：[Models](https://api-docs.deepseek.com/api/list-models)、[Chat Completions](https://api-docs.deepseek.com/api/create-chat-completion)、[Thinking Mode](https://api-docs.deepseek.com/guides/thinking_mode)。

## 数据存储与迁移

数据库名为 `lingua-step-learning`，当前 IndexedDB 版本为 **v3**，共 15 个对象仓库。

- v1：基础学习进度、错题、收藏、测试和每日记录。
- v2：三模式复习状态、每日计划和完整第二阶段数据。
- v3：AI 词汇、语法、对比、生成历史、用量、解释缓存、练习集和纠错记录。

升级在原数据库中通过事务完成，不清空旧库、不改变已有内容 ID。迁移失败时浏览器会回滚事务，应用降级到本次会话的内存仓库并保留原数据。

主题和非敏感设置保存在 localStorage；BYOK 和代理令牌使用独立 session/localStorage 键，不属于学习数据库。应用可通过 Web Locks 和 BroadcastChannel 合并并通知多标签页变更。

## 测试与质量检查

```bash
npm run typecheck
npm run lint
npm test
npm run test:components
npm run test:e2e
npm run build
npm run build:vercel
```

当前验收状态（2026-07-23）：

- TypeScript strict、ESLint、Vinext/Sites 构建和 Next.js/Vercel 构建通过。
- 19 个自动化测试文件，共 **132 项测试通过**。
- 测试覆盖 2559 组精选考试词/80 个语法/137 组对比/2160 道真题式练习的完整性、每种语言每个难度和题型 120 题的分布、真题语言与难度筛选、复习算法、每日计划、日期边界、随机学习顺序、三种学习模式、搜索筛选、测试判定、错题生命周期、统计聚合、设置、重置和 v1/v2/v3→v4 设置迁移。
- AI 测试覆盖配置、Provider、JSON Schema、业务校验、密钥隔离、接口保护、超时、取消、重试、组件和完整 Mock 工作流。
- 已使用真实 DeepSeek API 验证两个默认模型，以及词卡、日语语法、日英对比、练习题和错题解释全链路。

常规测试全部使用 Mock Provider，不访问 DeepSeek，不产生 API 费用。可选的最小真实冒烟测试：

```bash
DEEPSEEK_API_KEY=你的密钥 npm run test:deepseek
```

未提供环境变量时脚本会自动跳过；启用后可能产生少量 API 费用，且不会输出密钥。

## 部署

### Vercel

1. 将项目导入 Vercel，Framework Preset 选择 Next.js。
2. Build Command 使用 `npm run build:vercel`，Install Command 使用 `npm install`。
3. 在 Production/Preview 环境中配置 `.env.example` 对应变量。
4. `DEEPSEEK_API_KEY` 和 `AI_PROXY_ACCESS_TOKEN` 必须设为敏感变量，不能添加 `NEXT_PUBLIC_` 等客户端公开前缀。
5. 公网服务器 Key 模式必须同时配置代理访问令牌。
6. 部署后在设置页执行连接测试，并检查页面源码、Server Action 响应、`/study-service/health` 和错误响应，确认它们只显示是否配置，不包含密钥值。`/study-service/*` 与 `/api/ai/*` 作为兼容及诊断路径保留。
7. 修改 `DEEPSEEK_MODEL_FAST/QUALITY` 可以切换允许模型；设置 `AI_ENABLED=false` 可关闭 AI 而不影响本地学习。

服务端限流是单实例内存限流。在 Serverless 多实例环境中，它不是分布式全局配额；仍需结合代理访问令牌和 DeepSeek 账户配额。

### OpenAI Sites

项目包含 `.openai/hosting.json` 和 Vinext 构建，可发布到 OpenAI Sites。运行时环境变量应在 Sites 项目中配置，不要写入 `hosting.json` 或提交到仓库。

## 安全与隐私

- 服务器 Key 只从服务端环境变量读取，不进入客户端 Bundle。
- BYOK 和代理令牌不写入 IndexedDB、URL、日志、生成历史或测试快照；仅在用户发起 AI 操作时通过 HTTPS Server Action 临时传给服务端。
- 服务器校验同源、方法、请求大小、生成数量、字符串/数组长度和允许模型。
- AI 输出只作为普通文本数据处理，不执行代码、不作为 HTML 注入。
- 出题最多发送 100 条必要的学习摘要；错题解释只发送当前题和相关摘要。
- 不发送姓名、邮箱、完整数据库或无关学习历史。
- 删除 AI 内容会同时清理相关进度、收藏、错题、练习集和反馈引用。
- AI 内容可能有误，应结合可靠教材或官方资料判断。

## 版本阶段

| 版本 | 状态 | 主要内容 |
| --- | --- | --- |
| v0.1 | 已完成 | 首页、100 组词、20 个语法、基础学习/测试/错题/收藏/统计、IndexedDB v1、主题和响应式 |
| v0.2 | 已完成 | 300 组词、50 个语法、15 组对比、三模式复习、每日计划、搜索筛选、完整统计、IndexedDB v2 |
| v0.3 | 已完成 | DeepSeek 双 Key 模式、AI 内容生成/解释/校验/历史/用量、离线降级、IndexedDB v3 |
| v0.3.4 | 已完成 | Sites AI 通信切换为 Server Action/RSC，绕过托管层对普通 API fetch 的拦截并保留兼容路由 |
| v0.3.5 | 已完成 | AI 词汇/语法持续追加入库、全量双层去重、自动补足，以及学习页现场生成入口 |
| v0.4.0 | 已完成 | 单一今日学习主线、跨页面下一步、一键测试、连续错题队列和导航/筛选分层 |
| v0.4.1 | 已完成 | 新增学习专注模式开关，默认保留专注体验，也可让练习始终显示普通页面导航 |
| v0.4.2 | 已完成 | 测试先按内容去重、轮换六种翻译方向并打散词条，避免同一个词在一轮内重复出现 |
| v0.5.0 | 已完成 | 词库扩充至 6000 组、日英对比扩充至 137 组并接入混合测试、语法 AI 批量数量可调、错题卡进一步精简 |
| v0.5.1 | 已完成 | 统一日语动词词条、真题练习页重做、日英分级题库、日语文字/文法/阅读分类、学习与语法显式退出、移动端布局优化 |
| v0.5.2 | 已完成 | 精选词库扩充至 461 组、单词学习随机化、英文与日英混合筛选修复、学习页移除掌握筛选、测试即时反馈默认开启、统计页精简 |
| v0.5.3 | 已完成 | 语法列表与详情分层、真题测试分步配置、错题本和收藏卡片布局统一、移动端按钮与间距修复 |
| v0.5.4 | 已完成 | 修复错题筛选状态区被拉伸、收藏顶部操作与筛选结果行对齐问题 |
| v0.5.5 | 已完成 | 收藏页搜索框与语言、难度下拉框按输入控件底部统一对齐 |
| v0.5.6 | 已完成 | 语法详情改为 /grammar/:id 独立路由，测试拆分为 /test、/test/setup、/test/session 独立页面 |
| v0.5.7 | 已完成 | 按需求撤回语法与测试二级路由，恢复单页内完成浏览、配置和练习 |
| v0.5.8 | 已完成 | 移除左侧导航底部的当前目标卡片 |
| v0.6.0 | 已完成 | 按红/绿宝书对齐并扩充至 2559 组词（N1/N2/N3 每级至少 800 词），语法查找默认展开且难度简化为 N1/N2/N3 |
| v0.6.1 | 已完成 | 重构移动端导航、筛选抽屉和卡片间距；增加“更多”二级菜单分组，桌面端布局与数据结构保持不变 |
| v0.7.0 | 已完成 | 手机端彻底重制：独立应用外壳、单主操作首页、精简学习配置、纵向语法列表、移动筛选抽屉、低频管理收纳与分类设置；桌面端和数据库保持不变 |
| v0.7.1 | 已完成 | 测试支持选择 10、20、30、50 题，每次开始或重做都会从当前题库重新随机抽取 |
| v0.7.2 | 已完成 | 修复手机端导航漂移和专注模式跨页面残留；评分按钮常驻拇指区，首页增加精简的近期学习状态 |
| v0.7.3 | 已完成 | 修复单词总结页继续/重练流程，重练范围限定为本轮生词，并解决手机端评分区遮挡备注 |
| v0.7.4 | 已完成 | 测试选项与正确答案位置每轮重排并均衡分布；新增 Space 下一题/提交快捷键 |
| 后续 | 未承诺 | 账号与云同步、导入导出、发音、语音识别、自由文本批改、PWA 和完整 FSRS |

## 当前边界

项目目前不提供账号、云端学习进度、多设备同步、发音播放、语音识别、拼写输入、自由文本翻译/作文批改、AI 聊天陪练、数据导入导出、完整 PWA、管理员后台或社交功能。项目以个人、本地优先的日英学习为边界，不加入金币、排行榜或体力系统。

## 详细文档

- [架构说明](docs/architecture.md)
- [词汇数据来源与更新](docs/vocabulary-sources.md)
- [本地存储](docs/storage.md)
- [间隔重复算法](docs/spaced-repetition.md)
- [每日计划](docs/daily-plan.md)
- [v1/v2 → v3 数据迁移](docs/data-migration.md)
- [开发路线图](docs/roadmap.md)
- [DeepSeek 集成](docs/deepseek-integration.md)
- [AI 安全](docs/ai-security.md)
- [Prompt 管理](docs/ai-prompts.md)
- [AI 内容校验](docs/ai-content-validation.md)
- [AI 测试](docs/ai-testing.md)
- [AI 部署](docs/ai-deployment.md)
